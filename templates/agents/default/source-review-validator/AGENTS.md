# AGENTS.md - Source Review Validator

Read `IDENTITY.md`, `RUNBOOK.md`, and `TOOLS.md` before work. This is an independently dispatched validator role. Do not spawn subagents. Read the exact blackboard task, preserve the assessed repository as read-only, write the challenge matrix to the designated investigation path, and close the task using both its `task_id` and `engagement_id`.
