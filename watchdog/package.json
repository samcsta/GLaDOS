{
  "name": "glados-watchdog",
  "version": "0.1.0",
  "private": true,
  "description": "Shared target health, manual halt/resume, and plan-gate primitives for GLaDOS. Backs both the dashboard and the watchdog-mcp server.",
  "main": "lib/index.js",
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "engines": { "node": ">=20" }
}
